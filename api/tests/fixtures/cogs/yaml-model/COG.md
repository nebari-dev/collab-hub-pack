---
type: cog [0.1]
name: cog-small-model
description: Model-only Cog. Carries a small instruct model and the runtime needed to serve it over an OpenAI-compatible API.
version: "0.1.0"
manifest: cog.yaml
manifest_schema: openteams/cog-package [0.1]
---

# Small Model

The model half of the pair.
