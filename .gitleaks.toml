title = "collab-hub-pack gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Non-secret test fixtures (example values used by unit tests) and the throwaway localhost-only credentials of the dev compose stack"
paths = [
  '''api/tests/test_web_surface\.py''',
  '''dev/api\.env''',
  '''dev/keycloak/nebari-realm\.json''',
]
