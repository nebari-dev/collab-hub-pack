{
  "branches": ["main"],
  "tagFormat": "v${version}",
  "plugins": [
    "@semantic-release/commit-analyzer",
    [
      "@semantic-release/exec",
      { "publishCmd": "echo ${nextRelease.version} > .next-release" }
    ]
  ]
}
